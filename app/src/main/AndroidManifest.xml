<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        セキュリティポリシー:
        - INTERNET権限なし（完全オフライン）
        - USE_BIOMETRIC: 生体認証に必要
    -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
        ハードウェア要件:
        - StrongBox Keystore必須
        - 生体認証（指紋または顔認証）必須
    -->
    <uses-feature
        android:name="android.hardware.strongbox_keystore"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.biometrics.face"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Memoripass"
        tools:targetApi="31">

        <!--
            MainActivity: メインエントリーポイント
            - FLAG_SECURE: スクリーンショット・画面録画を防止
            - exported=true: ランチャーから起動可能
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>